language: php

php:
  - '7.3'

before_script:
  - composer install

script: phpunit

sudo: required

addons:
  apt:
    sources:
      - mysql-5.7-trusty
    packages:
      - mysql-server
      - mysql-client


before_install:
  - pwd
  - mysql -u root --password="" < tests/travis_database.sql